# 2026-08-14

## Hero 重排：参考图排版（左侧大字 + 右侧几何图形）
- 用户贴参考图（Patrick 风格作品集 hero），要求：左侧 Hello. I am [名字] 大字标题，右侧底部放几何图形组合，图形加动效。
- index.html：hero 从纯文字居中改为 flex 横排（.hero__left 文字列 + .hero__gfx 图形容器）；标题拆为三行 span（hero__greeting 含珊瑚色 dot / hero__verb / hero__name），保留 #hero-name 等 id 供 main.js 注入；SVG 内联 5 组图形：
  - ha-semi: 黑色半圆（右上）
  - ha-ripples: 4 圈同心圆波纹
  - ha-dot: 珊瑚色 #FF6B5B 实心圆
  - ha-dots: 两圈径向点阵圆（54 个小圆点）
  - ha-arc--1/--2: 底部黑色弧形
- css/style.css：Hero 改 `display:flex; align-items:center; gap` 横排；.hero__left flex:55%、.hero__gfx flex:40% 居底右对齐；字体大小分层（greeting 1em / verb 0.72em / name 1.15em，name 最粗 800）。
- 动效（纯 CSS keyframes，零依赖）：
  - haSemiFloat: 半圆缓慢上下浮动+微转（7s）
  - haRippleExpand: 波纹依次扩散放大（5s, stagger delay）
  - haDotPulse: 珊瑚圆脉冲呼吸（3.5s）
  - haDotsSpin: 点阵圆极慢旋转（20s）
  - haArcDrift1/2: 弧形交错漂移（6s/7s）
- 响应式：≤900px 纵向堆叠、图形绝对定位右下角缩小至 opacity:0.55；≤640px 进一步缩至 opacity:0.35。
- reduced-motion：全部动画禁用。

## 板块风格统一（精选作品 / 关于 → 对齐 hero）
- 用户认可 hero  redesign，要求下方两个板块（精选作品、关于）与之协调一致。
- 新增统一设计变量 `--accent: #FF6B5B`（珊瑚色），hero 的 `.hero__dot` 与 SVG `.ha-dot` 改引用 var(--accent)。
- 新增可复用 `.section-head` 组件（css/style.css）：小号英文副标 `.section-head__sub`（uppercase muted）在上、大号 800 字重中文标题 `.section-head__title` + 珊瑚圆点 `.section-head__dot` 在下，排版与 hero 三行字重对比呼应。
- 每个 section-head 右侧放 `.section-mark` 迷你几何标记（珊瑚实心圆 + 2 圈灰色波纹环，smRipple 缓慢呼吸动画），呼应 hero 图形语言；≤640px 隐藏。
- index.html：精选作品区 `<p class="label">` 与关于区 `<p class="label">` 均替换为 `.section-head`（含 section-mark SVG）。
- about.html：主标题由 `.label` + `.hero__title#about-name` 改为 `.section-head`（"关于." + 迷你标记），保持整站一致；#about-name 注入自动失效（无碍）。
- 链接 `.link-more:hover` 由 opacity:0.55 改为 变珊瑚色（color/border-color: var(--accent)）。
- `.lede` 放大到 clamp(1.5rem,3.6vw,2.4rem)、weight 600、letter-spacing -0.02em，更接近 hero 字体尺度。
- reduced-motion 追加关闭 `.section-mark .sm-ripple` 动画。

## Connect Me 联系区（替换原 footer 邮箱）
- 用户贴参考图（Connect Me 排版），要求删除底部邮箱部分，换成参考图的「联系我」区块，与现有网页风格统一。
- 参考图布局：左侧 "Connect Me" 大标题 + 描述文字 + 地区 badge；右侧顶部灰调建筑照片 + Email/Mobile 双卡片（含 QR）+ 底部黑色 CV 下载条。
- index.html：`<footer>` 中删除原 `.footer__mail` 邮箱链接；在 `</main>` 前新增 `<section class="connect">` 区块：
  - `.connect__title`：两行大字 "Connect / Me"，800 字重 clamp(2.8rem,7.5vw,5rem)
  - `.connect__lede`：描述段落（感谢浏览/24h 回复）
  - `.connect__badge`：圆角 pill 标签 "● BASED IN HONG KONG · GMT+8"
  - `.connect__photo`：内联 SVG 灰调几何抽象建筑光影图（渐变底 + 多边形 + 光影斜切）
  - `.connect__cards`：grid 两列卡片 — EMAIL（mailto 链接）+ MOBILE（静态含 SVG 二维码占位）
  - `.connect__cv`：黑色圆角条 "Curriculum Vitae / Download Portfolio (PDF)" + 下载图标
- css/style.css：新增完整 connect 区样式：
  - `.connect__body` grid 两列（1fr 1.1fr）；`.connect-card` 边框圆角 10px、hover 微阴影
  - `.connect__cv` 黑底白字、hover 上浮 translateY(-2px) + 阴影
  - 响应式：≤900px 单列堆叠；≤640px 卡片单列、Mobile 卡片横排（QR 右对齐）
- footer 保留精简版（仅 socials + copyright），padding 缩小。

## 关于区新增自我介绍段落
- 用户要求把一段详细自我介绍（逻辑清晰、注重落地的 UI 设计师…，输出可落地的多端设计方案。）放在关于区 lede「把复杂的业务问题…」下方。
- index.html 关于区：lede 后新增 `<p class="prose">` 段落（正文层级），保留下方「了解我的工作方式 →」链接。用的是内联 style margin-top:24px。

## OCR 项目图片替换为桌面素材
- 用户要求用 `/Users/youyu/Desktop/作品/OCR 项目` 文件夹里的图片替换网页 OCR Platform 项目的图片。
- 素材集与网站原 assets/OCR 的差异：素材多了 `3-1.svg`、无 `8.png`；1/2/3/4.png 等为更新版（尺寸/日期更新）。
- 操作：cp 素材文件夹内 *.png + *.svg → assets/OCR（覆盖旧图、补入 3-1.svg）；删除孤立的 8.png；更新 js/data.js 中 ocr 项目的 images 数组为 `1,2,3-1.svg,3,4,5,6,7,9.png`（cover 仍为 1.png）。
- 校验：3-1.svg 结构完好（1920x1334，含闭合 </svg>）；全站仅 data.js 引用 OCR 图片，无硬引用残留。
- 提醒：OCR/cpf/AES 三个项目的 desc 仍为占位文案，待用户补真实简介。

## 联系我板块删除二维码
- 用户要求把联系我部分的二维码删掉（页面唯一二维码在 Connect Me 的 Mobile 卡片 `.connect-card--static` 内 `.connect-card__qr` SVG 占位）。
- index.html：移除 Mobile 卡片内的 `.connect-card__qr` 整个 div（含 SVG）；Mobile 卡片现仅剩 label(Mobile) + value(15889891474)。
- css/style.css：删除失效的 `.connect-card__qr` / `.connect-card__qr svg` / hover 规则，以及 ≤640px 媒体查询里残留的 `.connect-card--static` 横排 + `.connect-card__qr` 规则，使 Email/Mobile 两张卡片在手机端表现一致（均为单列堆叠）。
- 注：本次会话早前出现的 502 ECONNREFUSED 127.0.0.1:1087 是本地代理连接被拒的网络错误，导致请求重试刷屏，与代码无关；网络恢复后任务正常完成。
