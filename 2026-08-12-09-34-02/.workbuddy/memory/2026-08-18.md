# 2026-08-18 工作日志

## Macau CPF Application 项目 meta 更新
- data.js cpf 项目：
  - 标题改为 "Macau CPF Application"
  - category 改为 "B端系统"
  - year 改为 "2026"
  - role 改为 "UX/UI设计师"
  - 新增 scope: "84 个页面"
- 详情页 meta 行自动渲染为：年份｜项目类型｜角色｜设计范围，值显示 "2026｜B端系统｜UX/UI设计师｜84 个页面"

## AES 项目详情页图片更新
- 将 Desktop/作品/AES 项目/ 下的 1.png、2.gif、3.png、4.png、5.png 复制到站点 assets/AES/
- data.js AES 项目 cover 与 images 数组更新为新文件列表：
  - assets/AES/1.png（封面）
  - assets/AES/2.gif
  - assets/AES/3.png
  - assets/AES/4.png
  - assets/AES/5.png

## AES 项目详情页文案更新
- desc 替换为正式项目简介，包含「项目背景」与「项目结果」两个章节

## CPF 项目详情页图片更新
- 将 Desktop/作品/cpf 项目/ 下的 1.png 到 8.png 复制到站点 assets/cpf/
- data.js CPF 项目 cover 与 images 数组更新为新文件列表（1.png 至 8.png），共 8 张

## CPF 项目详情页文案更新
- desc 替换为正式项目简介，包含「项目背景」与「项目结果」两个章节，阐述澳门强积金（CPF）业务流程整合至 SMART 系统的设计方案

## 首页 hero 角色文案更新
- data.js SITE.role 由 "视觉设计师 · 品牌 / 产品 / 动效" 改为 "UI/UX设计师 · AIGC / 产品 / 动效"

## 关于页 Lanyard 3D 挂绳卡片集成
- 新建 js/lanyard.js：基于 Three.js CDN 实现简化版 3D 挂绳卡片动画
  - 重力钟摆物理 + 阻尼 + 距离约束
  - 鼠标/触摸拖拽交互
  - CanvasTexture 生成卡片正反面纹理（Lynn Zhuang / Portfolio）
- about.html 修改：
  - 引入 Three.js CDN（r128）
  - lede 段落与 lanyard 容器使用 flex 并排布局（桌面端），移动端自动换行
  - 引入 js/lanyard.js 脚本

## 关于页 Lanyard 撤销恢复
- 用户反馈效果不佳，已撤销恢复
- 删除 js/lanyard.js，恢复 about.html 原有结构（移除 Three.js CDN、flex 容器、脚本引用）

## 关于页 互动表情脸（Uiverse face 移植）
- 新建 css/face.css：.about-intro 布局（lede 左 / 脸右，flex-wrap 响应式）+ 入场动画（眼睛浮现、鼻子下落、微笑画出）+ 循环眨眼（眼皮矩形 translateY）+ prefers-reduced-motion 兼容
- about.html：lede 包进 .about-intro，右侧内联 SVG 表情脸（viewBox 0 0 200 112，眼球/瞳孔/眼皮/鼻子/嘴两弧线），引入 face.css
- js/main.js：新增 initFace() —— 瞳孔跟随鼠标（atan2 求角度，距离映射 1.5~9 SVG 单位偏移，rAF 节流，支持 touchmove）
- 完全零依赖，纯 SVG + CSS + 原生 JS

## 作品页更新
- works.html 标题 "三个项目" → "近期作品"，字号减半（clamp(1.3rem, 4.5vw, 3.2rem)，内联覆盖 hero__title）
- main.js renderWorks()：PROJECTS 3 个卡片后追加 "04 往期项目" 外链卡片（assets/past-works.svg 封面，新标签跳转知设网）

## 页脚邮箱点击复制交互
- main.js：renderFooter 加 title="点击复制邮箱"；新增 copyText()（clipboard API + execCommand fallback，file:// 兼容）、showCopyTip()（fixed 定位浮窗跟随鼠标位置，防屏幕边缘溢出）、initMailCopy()（preventDefault mailto，复制后 1.6s 浮窗提示"已复制"）
- style.css 末尾新增 .copy-tip 样式（黑底白字圆角胶囊，淡入淡出 + 上移动画）
- 全站页脚生效（所有页面共用 main.js）

## 首页联系区改为纯文字展示
- index.html：Email 卡片由 `<a href="mailto:">` 改为静态 div（与 Mobile 卡片一致）；Download Portfolio 由 `<a href="#" download>` 改为静态 div，保留文字与图标
- style.css：删除 connect-card 与 connect__cv 的 hover 反馈及过渡规则（纯展示不再有可点击暗示）
